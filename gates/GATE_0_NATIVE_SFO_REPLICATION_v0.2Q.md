# Gate 0 — v0.2Q SFO replication after composition review

**Status:** OFFLINE PREPARATION COMPLETE; OWNER STAGE-A SFO REPLICATION REQUIRED.

## Closed offline facts

- Fresh v0.2P SFO upload is confirmatory-eligible and reproducible.
- Frozen temporal branch = `HIGHER_THAN_VANILLA_CLUSTER_ENVELOPE`.
- Frozen recovery branch = `SFO_RECOVERY_NO_PREREGISTERED_SIGNAL`.
- SFO aggregate temporal rate remains above the vanilla envelope under every single-faction deletion.
- Shared-faction post-result review reverses direction and exposes serious campaign-composition uncertainty.
- No native-row ablation is earned.

## Next owner evidence

One fresh SFO replication only. Fresh vanilla is conditional on Stage A replicating the elevated SFO aggregate.

Authority remains `NO_ORDERS`; application remains `PROHIBITED`; privileged telemetry is research-only.
