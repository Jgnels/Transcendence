# Gate 0 — Tactical Feasibility Envelope Report v0.1T

## Result

**CLOSED OFFLINE.** Owner-machine detailed re-export remains the smallest next evidence task and requires no replay or new battle.

## Canonical base

- Owner-validated release: v0.1S.
- Base canonical files: 239.
- Base validation: 56 SyntheticLab + 73 Runtime Probe = 129 tests.
- Public GitHub seed remains unchanged at `16c4fa0d5290822a9fcba150b4dffcc8152f1b52`.

## Implemented

- bounded role-aware candidate-point generation;
- strict schedule/state authenticity checks;
- exact point-query evidence binding;
- fail-closed query-veto and unavailable classifications;
- aggregate live capability profile kept separate from candidate evidence;
- 15-scenario feasibility matrix and three representation invariants;
- no-replay detailed re-export parser, PowerShell workflow, verifier, and public-safe fixture.

## Battle 4 result

- eight canonical slices;
- 34 active-plan envelopes;
- 102 bounded candidate points;
- maximum three candidates per plan;
- maximum 120 m abstract displacement;
- 34 `QUERY_READY_NOT_OBSERVED` envelopes;
- zero point-supported or point-rejected Battle 4 envelopes because no exact candidate query evidence exists.

## Synthetic result

- 15/15 scenario contracts passed;
- 3/3 metamorphic checks passed;
- seed `20260730`;
- no issue, acknowledgement, route, execution, or outcome promotion.

## Defects prevented

1. **Aggregate-to-instance leakage:** one battle's reachability prevalence cannot be assigned to unrelated candidate points.
2. **Point-to-route overclaim:** `QUERY_TRUE` cannot become route completion, formation feasibility, safe arrival, or order legality.
3. **False universal veto:** `QUERY_FALSE` rejects only the exact point rather than an objective or region.
4. **Forged evidence binding:** candidate, actor, observation time, state digest, schedule digest, and query-evidence digest must reconcile.
5. **Unnecessary replay:** the exact preserved v0.1S raw log can be re-exported into a richer public-safe packet without launching WH3.

## Evidence classification

- Envelope implementation: `CONTROL_OFFLINE`.
- Matrix: `CONTROL_SYNTHETIC`.
- v0.1S query availability: scoped `OBSERVED` aggregate capability.
- Battle 4 candidate points: `QUERY_READY_NOT_OBSERVED`.
- Detailed owner-log re-export: fixture-verified; owner-machine run pending.
- Authority: `NO_ORDERS`.

## Validation

- 61 SyntheticLab tests passed;
- 78 Runtime Probe tests passed;
- 139 total tests passed twice;
- 255 canonical repository hashes validated;
- all four generated evidence artifacts rebuilt byte-identically twice;
- Python compilation passed;
- authority remained `NO_ORDERS`.

## Remaining uncertainty

Terrain paths, formations, collision, action legality, command issue/acceptance/acknowledgement, execution, interruption causality, arrival, casualty reduction, tactical superiority, SFO, sieges, reinforcements, monsters, flying units, and faction generalization remain unverified.

## Smallest next task

After installing v0.1T, run `runtime_probe/tools/reexport_action_feasibility_capture.ps1` once and upload the generated `Transcendence_ActionFeasibilityWindows_<timestamp>.zip`. No game launch or repeat battle is required.
