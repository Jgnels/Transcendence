# GATE 0 — Native CAI Reconciliation

**Status:** ACTIVE RESEARCH GATE  
**Authority:** `NO_ORDERS`  
**Application authority:** `PROHIBITED`  
**Owner live WH3 work:** not authorized/needed until the offline phases below are substantially complete.

## Purpose

Determine exactly which strategic responsibilities current WH3 8.1.x already owns, which are publicly tunable, which require bounded scripting, which Transcendence should only measure, and which—if any—the project must own.

## Gate phases

### Phase 0 — preserve and pin — CLOSED
- v0.2I-r3 authoritative baseline frozen by SHA-256.
- 13 verbatim Grok/Kimi/Claude reports preserved with corrections.
- native-first architecture decision recorded.
- v0.2I assignment-coupled path paused.

### Phase 1 — exact 8.1 schema + vanilla rows — OPEN
Acquire the exact `schema_wh3.ron` from RPFM schema commit `d12f59cb6de106d205f51b81739951adbc840c49` (`Updated wh3 schemas for patch 8.1`) and export current vanilla rows for the priority CAI families listed in `research/source_audits/NATIVE_CAI_RECONCILIATION_WORKPLAN.md`.

### Phase 2 — mod row diffs — OPEN
Normalize and diff vanilla → SFO → DeepWar → Hecleas → Incata/narrow mods under one schema. Distinguish strategic CAI changes from budgets/potential/difficulty/recruitment/environment/scripted bypasses.

### Phase 3 — Patch 8.1 priority exposure — OPEN
Classify the new turn-dependent prioritisation mechanism as `DB_EXPOSED`, `SCRIPT_EXPOSED`, `ENGINE_INTERNAL`, or `UNVERIFIED`. Official behavioral existence is established; modder exposure is not yet established.

### Phase 4 — responsibility reconciliation — OPEN
For each v0.2E-I responsibility assign exactly one working disposition:
- `ALREADY_NATIVE`
- `TUNABLE_NATIVE`
- `SCRIPT_CORRECTABLE`
- `PROJECT_SHOULD_MEASURE`
- `PROJECT_MUST_OWN`
- `UNAVAILABLE_OR_UNVERIFIED`

### Phase 5 — native-first ablation design — BLOCKED ON PHASES 1–4
Design owner runs only around observables that can change the architecture. Hidden native task IDs/priorities may not be assumed observable.

## Gate closure criteria

This gate is not closed until:
1. exact schema identity is byte-verified;
2. vanilla priority-family row exports exist with hashes;
3. SFO/DeepWar/Hecleas row-level deltas are classified;
4. Incata and at least two narrow contemporary AI-mod surfaces are acquired or explicitly marked unavailable;
5. Patch 8.1 exposure is classified without inventing a table/variable;
6. every strategic responsibility receives a disposition with evidence;
7. the first live ablation is preregistered with explicit falsification/deletion thresholds.

## Hard prohibitions while active

- no v0.2I-r4 or further assignment-derived transport work;
- no project campaign orders;
- no tuning of v0.2F/v0.2G controller constants for application;
- no new strategic director, rival/bloc controller, or army allocator;
- no promotion of historical/beta numeric values to current 8.1 truth without row/runtime evidence.


## 2026-08-02 owner-row progress

`PARTIAL_PASS — HIGH-VALUE ROW RECONCILIATION COMPLETE`

Completed offline from owner-acquired current data:
- exact binary inventory for 26 vanilla target tables and major-mod overlaps;
- nine high-value row families fully decoded and deterministically diffed;
- current allocator distance/recruit/release/return surfaces proven in vanilla data;
- current timed/endgame priority rows proven in vanilla data;
- current player-specific threat/strength multiplier rows proven;
- DeepWar/Hecleas/SFO task-priority deltas measured;
- prior DeepWar file-size inference retracted.

Gate remains open only for the unresolved evidence items: exact pinned schema-byte/key certification, narrow-mod acquisition where valuable, and later controlled behavioral ablations. No v0.2I restart is authorized by this progress.
