# Gate 0 — Native CAI Behavior Acquisition v0.2K

**Date:** 2026-08-02  
**State:** `OFFLINE_ACQUISITION_BOUNDARY_PREPARED / HISTORICAL_OWNER_REPROCESS_LIMITING_RESULT`  
**Authority:** `NO_ORDERS`  
**Application:** `PROHIBITED`

## Gate question

Can Transcendence acquire useful native-CAI behavioral evidence without either:

1. reviving the project-owned strategic assignment stack; or
2. reading foreign information that WH3 does not expose to the human player?

## Result

**YES, partially.**

The existing shadow probe already exposes a safe subset sufficient for longitudinal player-visible foreign-force movement analysis. v0.2K adds a separate partial-observation evaluator and runtime adapter that refuses to convert missing foreign state into negative evidence.

## Closed offline checks

- [x] foreign provenance is pinned to `WH3_PLAYER_FILTERED_LISTS`;
- [x] at least two chronological frames are required;
- [x] observed AI faction must be foreign to the human observer;
- [x] continuously visible foreign forces use stable observed force CQI when available;
- [x] visibility loss is right-censored rather than treated as destruction/reassignment;
- [x] stationary position is not promoted to AI idleness;
- [x] ambiguous movement does not invent a target;
- [x] directional-anchor changes are review candidates, not task churn;
- [x] full front coverage/response latency are unavailable under incomplete foreign visibility;
- [x] recovery misuse is unavailable without safe engagement-time replenishment;
- [x] reserve adequacy is unavailable under incomplete foreign visibility;
- [x] assignment exclusivity, native memory, and hysteresis remain unavailable;
- [x] the v0.2K runtime adapter does not import the old strategic assignment pipeline;
- [x] no campaign order/application surface was added.

## Historical owner-evidence reprocess

Source:

`research/runtime_evidence/CAMPAIGN_TURNS_4_7_REPROCESS_v0.1J.json`

Derived artifact:

`research/runtime_evidence/PLAYER_VISIBLE_NATIVE_BEHAVIOR_REPROCESS_v0.2K.json`

Digest:

`5d2e6fdf9f2286225815842488977d631d31dae49c74acae22e27f025e6e831d`

Result:

- 7 foreign AI factions;
- turns 4–7;
- 37 comparable visible-force intervals;
- 36 position-stable intervals;
- 1 movement interval;
- 1 ambiguous-anchor movement;
- 0 unambiguous directional-anchor proxies;
- 0 direction-change candidates;
- 6 visibility gains and 6 visibility losses.

This is a **limiting result**. It does not falsify native CAI and does not establish good native behavior. It shows the available historical cohort is too position-stable and context-incomplete to evaluate native task churn from movement.

## Gate interpretation

A full-information v0.2J metric must not be run over a player-visible partial trace and silently interpreted as complete. The two evidence modes stay separate:

- `NATIVE_CAI_BEHAVIOR_TRACE_V1`: complete/synthetic reference contract for metric semantics;
- `NATIVE_CAI_PLAYER_VISIBLE_PARTIAL_TRACE_V1`: actual observer-safe foreign-AI acquisition contract.

## Certification

Complete v0.2K test run on 2026-08-02:

- SyntheticLab: **159/159 PASS**
- Runtime Probe: **164/164 PASS**
- Combined: **323/323 PASS**

Repository hash-manifest validation is performed after this report and the other canonical docs are finalized.

## Next permissible work

Offline:

1. use the v0.2K availability matrix to select one preregistered native-failure question;
2. prefer metrics answerable from player-visible evidence;
3. identify whether existing owner evidence already answers it;
4. only then prepare a new read-only owner observation if necessary.

Not permitted:

- resuming assignment-derived v0.2I;
- treating v0.2G assignments as native-ground-truth labels;
- enumerating hidden foreign force lists in the normal observer;
- issuing campaign orders;
- interpreting position stability alone as an AI defect.
