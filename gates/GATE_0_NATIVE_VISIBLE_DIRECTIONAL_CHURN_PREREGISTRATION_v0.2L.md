# Gate 0 — Native Visible Directional Churn Preregistration v0.2L

**Date:** 2026-08-02  
**State:** `PREREGISTERED_OFFLINE / OWNER_CAPTURE_HARNESS_READY / LIVE_CAPTURE_NOT_RUN`  
**Authority:** `NO_ORDERS`  
**Application:** `PROHIBITED`

## Gate question

Can Transcendence define a falsifiable player-visible native-CAI churn signal before another owner WH3 session, without inferring hidden tasks or reviving project strategic control?

## Answer

**YES.**

The frozen primary endpoint is a repeated, region-anchored A→B→A directional oscillation cluster under exact stability of all observed non-target anchors. It requires two non-overlapping candidate episodes for the same foreign force and same region pair.

## Closed offline checks

- [x] endpoint is frozen before new owner data;
- [x] same foreign force must remain continuously player-visible;
- [x] local-player turns must be consecutive;
- [x] each movement interval must have one unique visible-region approach proxy;
- [x] primary pattern is A→B→A;
- [x] each heading change must be >=120 degrees;
- [x] all observed non-target anchor identities/owners/positions must remain exact across the four-frame candidate window;
- [x] one candidate cannot trigger the primary signal;
- [x] primary signal requires two non-overlapping episodes for the same actor/region pair;
- [x] visibility loss is right-censored;
- [x] ambiguous/unanchored/stationary intervals cannot become positive evidence;
- [x] privileged foreign-faction enumeration is rejected;
- [x] hidden context remains explicitly unavailable;
- [x] native task identity, assignment memory and engine hysteresis remain unavailable;
- [x] comparison output is descriptive only;
- [x] positive signal cannot authorize project strategic planning;
- [x] no order/application path added.

## Adversarial matrix

Frozen suite contains 9 cases covering:

1. repeated clean oscillation;
2. a single A→B→A candidate that must not trigger the primary signal;
3. sustained one-direction travel;
4. observed context motion;
5. nonconsecutive turns;
6. visibility censoring;
7. ambiguous anchor movement;
8. privileged visibility rejection;
9. self-faction observation rejection.

Current result: **9/9 PASS**.

## Historical owner evidence

The turns 4–7 cohort yields:

- eligible stable-context region windows: 0;
- oscillation candidates: 0;
- repeated clusters: 0.

Classification:

`SUPPORTED_DERIVED_OWNER_EVIDENCE_LIMITING_RESULT`

This does not test the primary hypothesis because the denominator is zero.

## Owner-capture harness closure

- [x] exact VANILLA/SFO profile binding is prelaunch hash-bound;
- [x] vanilla rejects every non-probe mod;
- [x] SFO requires exactly one SFO Workshop pack plus the shadow probe;
- [x] SFO pack mutation after binding fails closed;
- [x] deferred local-probe launcher materialization is modeled narrowly and still requires runtime marker + exact staged/installed hash;
- [x] consecutive-turn and observer-faction checks are enforced;
- [x] passive owner protocol is attested and non-attestation makes evidence nonconfirmatory;
- [x] public upload excludes local paths/private launcher copies/raw Workshop pack bytes;
- [x] VANILLA/SFO comparison requires identical campaign protocol and is descriptive only;
- [x] owner PowerShell wrappers contain only Python discovery/invocation;
- [x] fake-owner VANILLA and SFO capture flows reach verification and deterministic upload packaging;
- [x] the standalone owner-kit ZIP is deterministic, contains no `.pack` bytes/private paths, and was exercised after extraction for both profiles.

## Next step

Run **VANILLA first** under the frozen 12-turn passive protocol. If vanilla produces zero eligible stable-context region windows, do not request SFO; redesign the acquisition protocol. If vanilla produces at least one eligible window, a matched SFO run may be requested without changing the endpoint.
