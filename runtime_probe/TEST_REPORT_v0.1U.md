# Runtime Probe Test Report — v0.1U

## Scope

Exact no-replay detailed export adjudication and command-position semantic qualification.

## Added coverage

- exact observed windows and manifest reproduce the frozen semantic calibration;
- all 38 raw false samples are traced to zero-vector formation callback window `w5`;
- 191 explicit-point samples remain qualified true evidence;
- zero-vector `Move` input fails closed;
- project issue claims fail closed;
- raw and qualified evidence counts remain separate;
- source ZIP, window, manifest, raw-log, and pack identities are exact.

## Privacy and authority

The frozen detailed export contains no raw log lines or personal paths. Project issue and direct acknowledgement counts remain zero. Authority remains `NO_ORDERS`.

## Result

81 Runtime Probe tests passed.
