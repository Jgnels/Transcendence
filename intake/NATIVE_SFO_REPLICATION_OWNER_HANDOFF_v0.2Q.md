# README FIRST — v0.2Q SFO replication Stage A

This is a **fresh replication**, not a continuation of the prior campaign.

1. Run `runtime_probe\tools\Prepare-NativeSFOReplication.ps1`.
2. Confirm the launcher has exactly SFO main + `transcendence_native_diagnostic_probe.pack` and no other mods.
3. Start a NEW disposable Karl Franz/Reikland Immortal Empires campaign on Legendary / Very Hard.
4. Play normally; autoresolve is allowed.
5. Reach the start of Reikland turn 11 (10 complete AI cycles minimum).
6. Exit WH3 completely.
7. Run `runtime_probe\tools\Collect-NativeSFOReplication.ps1`.
8. Upload the resulting SFO behavior ZIP to ChatGPT.

Do **not** run another vanilla campaign yet. A fresh vanilla replication is only useful if this Stage-A SFO replicate again exceeds the frozen 31.7073% territorial cluster ceiling with sufficient exposure and no repeated territorial special-policy trigger.

The probe is privileged research telemetry only. It issues no orders and is permanently application-ineligible.
