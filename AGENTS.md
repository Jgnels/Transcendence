## v0.2Q binding rule — SFO benchmark adjudication and staged replication

The fresh owner SFO benchmark bundle SHA-256 `e2e8bf8ceb1ef3e365586cb428a420210530dcedfe6e96901b3d794b3fb4ca04` is confirmatory-eligible and reproduces sealed v0.2P result digest `af4056f3ce2d184dea29444edb5b049655c4d767b056ea448b97d45b5a65dbbb`. Preserve the frozen v0.2P branches unchanged: recovery = no signal; territorial temporal = `HIGHER_THAN_VANILLA_CLUSTER_ENVELOPE` / descriptive worsening / replicate and causal-review / do not copy SFO priority increases. Causal review finds the SFO aggregate robust to every single-faction deletion but campaign composition unstable: only 6 eligible territorial factions overlap across the one vanilla and one SFO campaign, and shared-faction direction reverses. Therefore no row ablation is earned. Apply `research/native_cai/NATIVE_SFO_REPLICATION_DECISION_POLICY_v0.2Q_2026-08-04.json`: request one fresh SFO Stage-A replication first; request a fresh vanilla replicate only if SFO elevation independently replicates. Never reinterpret the existing SFO run after the fact, never copy SFO rows wholesale, never use privileged telemetry in application, and never revive v0.2G/v0.2I. Authority remains `NO_ORDERS / PROHIBITED`.

## v0.2P binding rule — pre-SFO mechanistic precommitment

Before any fresh SFO behavior data, apply `research/NATIVE_SFO_MECHANISTIC_PRECOMMITMENT_v0.2P.md` and `research/native_cai/SFO_POST_BENCHMARK_DECISION_TABLE_v0.2P_2026-08-04.json`. Treat the vanilla territorial 0.222222–0.317073 leave-one-faction-out range as descriptive cluster sensitivity only, never a confidence interval or p-value. The captured SFO CAI footprint contains 88/88 changed task-priority rows as increases across 30 generator groups and no decoded SFO allocator-variable override; therefore do not directly attribute an SFO recovery difference to allocator release/return rows. Any favorable SFO difference can nominate replication only; any row ablation remains dormant until replication and separate preregistration. Never copy SFO rows wholesale, never use privileged telemetry in application, and do not revive v0.2G/v0.2I. Authority remains `NO_ORDERS / PROHIBITED`.

## v0.2O binding rule — matched SFO behavior benchmark

Treat the fresh v0.2N vanilla endpoint result and v0.2O causal review as separate evidence layers. Preserve the formal vanilla result: recovery had sufficient exposure but no preregistered signal; temporal produced one repeated-force signal. Do **not** promote that temporal result into ordinary territorial-CAI thrashing: the sole repeated trigger was `wh2_dlc11_cst_rogue_grey_point_scuttlers`, a non-territorial Rogue Pirate patrol candidate with no owned regions/wars in the qualifying windows. No ordinary native-row ablation is authorized from v0.2N alone. For fresh SFO evidence, prospectively use stable non-empty owned-region windows as the primary territorial strategic cohort and report empty-region roaming/patrol windows separately, while retaining all v0.2N thresholds unchanged. Bind SFO to Workshop `2792731173` / `sfo_grimhammer_3_main.pack` SHA-256 `ae20a0a08037aa3ebcbe7461d2589fc28dc15a5fe5d9ca4c0a9d0ff0a26da603` and reject drift/extra mods. Privileged telemetry remains research-only and `application_eligible=false`; authority remains `NO_ORDERS / PROHIBITED`.

## v0.2N binding rule — fresh confirmatory native behavior study

v0.2M-r1 telemetry qualification is complete: the successful owner vanilla capture produced 1,588 paired AI faction-turns, 2,353 matched force-turn pairs, 967 moved pairs, 426 trajectory pairs, zero capability failures and zero incomplete normal AI faction turns. Treat that cohort as **exploratory only** for behavior because v0.2N endpoints were refined after inspecting it and the old probe had no battle-participant channel.

The active gate is `gates/GATE_0_NATIVE_DIAGNOSTIC_BEHAVIOR_STUDY_v0.2N.md`. The confirmatory evaluator must refuse traces that do not declare `battle_participant_telemetry=true`; this structurally prevents retrospective “battle-free” claims from v0.2M-r1. v0.2N uses a fresh vanilla cohort, 10 complete AI cycles / 11 consecutive Reikland turn-start markers, normal play/autoresolve allowed, exact one-probe launcher binding, and the frozen threshold digest `4499a497bfc7dd6a33a6c0a50da4e1e52b1705d1f2f20f1a917cdfebeb5486ac`.

Positive recovery or temporal signals can earn causal review and then the smallest mechanistically relevant **native-row ablation only**. They cannot directly revive v0.2G/v0.2I or authorize a project strategic planner. Privileged diagnostic inputs remain `application_eligible=false`; application remains player-visible/native-first and `NO_ORDERS / PROHIBITED`. Request a matched SFO cohort only after confirmatory-eligible vanilla and only if at least one endpoint has sufficient exposure.

## v0.2M binding rule — diagnostic telemetry is research-only

The v0.2L vanilla owner capture had zero eligible player-visible churn windows, so do not request its SFO match. v0.2M separates normal player-visible application evidence from privileged development telemetry. `transcendence_native_diagnostic_probe` may enumerate complete AI faction forces only for offline research and is permanently `application_eligible=false`, `NO_ORDERS`, `PROHIBITED`. Never wire privileged diagnostic state into gameplay decision/application code.

## 2026-08-02 POST-REVIEW ARCHITECTURE OVERRIDE — HIGHEST PRIORITY

The external native-CAI reconciliation review supersedes the earlier default of continuing `v0.2E→F→G→H→I` toward project-owned strategic control. Native WH3 CAI is now the default application planner/executor until falsified. v0.2E/F/G remain evaluator/research artifacts; v0.2H must be generalized away from assignment coupling before future application use; assignment-derived v0.2I is PAUSED. Do not build v0.2I-r4, tune v0.2G controller constants, issue campaign orders, or add another strategic controller while `gates/GATE_0_NATIVE_CAI_RECONCILIATION.md` is active. Read `research/NATIVE_CAI_RECONCILIATION_DECISION_2026-08-02.md` and the active gate before interpreting older version-specific rules below. Older rules remain historical evidence unless they conflict with this override.

## v0.2K player-visible native behavior acquisition rule — ACTIVE OFFLINE GATE

Treat `research/NATIVE_CAI_BEHAVIOR_ACQUISITION_SPEC_v0.2K.md`, `gates/GATE_0_NATIVE_CAI_BEHAVIOR_ACQUISITION_v0.2K.md`, and `research/runtime_evidence/PLAYER_VISIBLE_NATIVE_BEHAVIOR_REPROCESS_v0.2K.json` as the current acquisition boundary. Foreign AI observations in the normal owner-safe path must originate from `WH3_PLAYER_FILTERED_LISTS`; do not enumerate complete foreign faction force lists merely because the scripting model can expose them. Under partial visibility, position stability and visible-anchor direction are proxies only. Full front coverage, response latency, recovery misuse, reserve adequacy, task identity, exclusivity, assignment memory, and hysteresis remain unavailable unless a later evidence contract closes those gaps. The historical turns 4–7 reprocess is a limiting result, not proof native CAI is idle or good. No owner run is requested by v0.2K. Preserve `NO_ORDERS` / `PROHIBITED` and keep v0.2G/v0.2I out of the application path.

## v0.2J native-behavior observation rule — ACTIVE OFFLINE GATE

Treat `research/NATIVE_CAI_BEHAVIOR_OBSERVATION_SPEC_v0.2J.md`, `synthetic_lab/scenarios/native_cai_behavior_adversarial_matrix_v0.2J.json`, and its frozen result as the current bridge from native-CAI row reconciliation to future behavior measurement. v0.2J is trajectory-only and `NO_ORDERS` / application `PROHIBITED`. Movement direction is a target proxy, not native task telemetry. A healthy unengaged home-zone force is a reserve-capacity proxy, not proof of native reserve policy. Recovering-army misuse requires an observed offensive engagement with actor replenishment measured at engagement time. Assignment exclusivity, native assignment memory, and native hysteresis remain unavailable from trajectory data alone. Do not resurrect v0.2G/v0.2I, invent native task identities, or request an owner WH3 run until a separate read-only observation acquisition contract is prepared and visibility limitations are explicit.

## v0.2I-r3 active embedded-request handoff rule

The third owner-runtime retry closed the remaining file-handoff ambiguity far enough for architecture: the r1 real/UI poll fires and the r2 parser progresses, but WH3 still does not emit `FEASIBILITY_REQUEST_SEEN` for the sidecar-created relative request. Treat `research/runtime_evidence/CAMPAIGN_FEASIBILITY_LIVE_OBSERVATION_LIMITING_RESULT_v0.2I-r3.json` as the bounded owner-runtime limiting result. Do not keep retrying runtime-created inbound files. Use `prepare_campaign_feasibility_embedded_retry.ps1` to generate a session-local read-only PFH5 pack with the exact saved request embedded before WH3 starts. Evidence is accepted only if the new observer-safe snapshot independently reproduces the same saved v0.2H plan. Preserve `NO_ORDERS` / application `PROHIBITED`. Generated retry packs are local evidence tooling, not canonical repository members.

## v0.2I-r2 active parser-contract hotfix rule

Treat `research/runtime_evidence/CAMPAIGN_FEASIBILITY_LIVE_OBSERVATION_LIMITING_RESULT_v0.2I-r2.json` as the exact second owner-runtime limiting result and `CAMPAIGN_STRATEGIC_FEASIBILITY_LIVE_PREPARATION_v0.2I-r2.json` as the active preparation contract. The r1 real/UI timer is now owner-runtime supported because `FEASIBILITY_POLL_TICK` was observed. The remaining defect was local parser drift: every static `trans_probe_emit("...")` event in the dedicated campaign-feasibility Lua must be accepted by the shared log parser before a live request can be derived. Preserve the `NO_ORDERS` / application `PROHIBITED` boundary and do not alter the game-side probe merely to fix local transport parsing.

# AGENTS.md — WH3 AI Project Constitution

## v0.2I-r1 active hotfix rule

Treat `research/runtime_evidence/CAMPAIGN_FEASIBILITY_LIVE_OBSERVATION_LIMITING_RESULT_v0.2I-r1.json` as the exact first owner-runtime limiting result and `CAMPAIGN_STRATEGIC_FEASIBILITY_LIVE_PREPARATION_v0.2I-r1.json` as the active preparation contract. The first live attempt proved snapshot/plan/request generation but not query execution: `FEASIBILITY_EXECUTOR_READY=1`, five bound queries requested, and zero results/rejection/packet-end. The request executor must use an immediate poll plus `repeat_real_callback` (UI-synchronized real timer), never the old model-synchronized `repeat_callback`, so the owner is not required to advance the campaign model. Preserve `FEASIBILITY_POLL_TICK` and `FEASIBILITY_REQUEST_SEEN` diagnostics. Authority remains `NO_ORDERS`; application remains `PROHIBITED`.

## v0.2H active release rule

Treat `research/runtime_evidence/REIKLAND_CAMPAIGN_STRATEGIC_FEASIBILITY_ENVELOPE_v0.2H.json`, `synthetic_lab/scenarios/campaign_strategic_feasibility_adversarial_matrix_v0.2H.json`, and its frozen result as the current campaign feasibility boundary. A documented WH3 query surface is not owner-runtime proof. Faction centroids remain point references only; REGION-to-settlement resolution permits reachability queries but never promotes an attack target. `can_assault` is context-ambiguous and must not be used as actor-target attack legality. All preserved query results remain `UNOBSERVED_QUERY_NOT_RUN`; authority is `NO_ORDERS`, application `PROHIBITED`. Campaign movement/attack/stance/action-point/pathfinding mutation surfaces are explicitly prohibited until a later separately authorized gate.

## v0.2G active release rule

Treat `research/runtime_evidence/REIKLAND_CAMPAIGN_STRATEGIC_FORCE_ALLOCATION_v0.2G.json`, `synthetic_lab/scenarios/campaign_theater_assignment_commitment_adversarial_matrix_v0.2G.json`, and its frozen result as the current campaign force-allocation boundary. Army assignments are `SHADOW_PROPOSED_NOT_EXECUTED`; straight-line geometry is `NOT_EVALUATED_GEOMETRIC_REFERENCE_ONLY`, never path feasibility. Preserve one abstract army commitment slot per actor, recovery/reserve protection, explicit critical-only recovery override, critical-overflow expansion, and the 2/4-turn anti-thrashing lifecycle. Retirement receives `NO_CAUSAL_CREDIT`. Authority remains `NO_ORDERS`; application remains `PROHIBITED`. Do not request a campaign session merely to re-prove this offline gate.

## v0.2F active release rule

Treat `research/runtime_evidence/REIKLAND_CAMPAIGN_STRATEGIC_THEATER_PORTFOLIO_v0.2F.json`, `synthetic_lab/scenarios/campaign_strategic_theater_portfolio_adversarial_matrix_v0.2F.json`, and its frozen result as the current strategic-priority boundary. The portfolio is bounded to six top-level records, preserves all critical sources through explicit overflow, aggregates fragmented pressure instead of rewarding war count, and may veto new aggressive commitment during a front crisis or total-force recovery. `CONTAIN_COHERENT_VISIBLE_RIVAL` is a theater priority only, never an order, assignment, route, coordination claim, or anti-player target. Human/player identity remains semantically inert. Authority remains `NO_ORDERS`; application remains `PROHIBITED`. Do not request another campaign session to re-prove this offline gate.

## v0.2E active release rule

Treat `research/runtime_evidence/REIKLAND_CAMPAIGN_STRATEGIC_CHALLENGE_ENVELOPE_v0.2E.json`, `synthetic_lab/scenarios/campaign_challenge_adversarial_matrix_v0.2E.json`, and `synthetic_lab/results/campaign_challenge_adversarial_matrix_v0.2E.json` as frozen project-owned strategic benchmark artifacts. Preserve `NO_ORDERS` / application `PROHIBITED`, observer-safe hidden-army filtering, player-label invariance, and the distinction between co-located pressure and proven coordination. `COHERENT_VISIBLE_RIVAL_CANDIDATE` is a snapshot benchmark label, not evidence of durable rival power, diplomacy, native CAI quality, or anti-player bias. The 2.0-turn, 0.65-replenishment, 0.50-coherence, and 0.65-fragmentation thresholds are engineering definitions, not empirical truths. Do not request another battle/replay to advance this campaign gate.

## v0.2D active release rule

Treat `research/runtime_evidence/REIKLAND_CROSS_CORPUS_TACTICAL_POLICY_ENVELOPE_v0.2D.json`, `synthetic_lab/scenarios/tactical_policy_adversarial_matrix_v0.2D.json`, and `synthetic_lab/results/tactical_policy_adversarial_matrix_v0.2D.json` as frozen project-owned offline control artifacts. Preserve the exact four-corpus provenance and the explicit Ubersreik/Marienburg/Chaos runtime-identity alias cohort. The policy is `NO_ORDERS`, application is `PROHIBITED`, target concentration remains a single-corpus hypothesis, and the severe-loss ratio is an advisory engineering threshold rather than causal tactical truth. Do not request repeats of the four source replays to re-prove this gate.

## v0.2C active release rule

Treat `runtime_probe/fixtures/sfo_chaos_replay_stream_capture_v0.2C.json`, `runtime_probe/fixtures/sfo_chaos_replay_unit_findings_v0.2C.json`, `synthetic_lab/corpora/sfo_chaos_replay_stream_observed_dense_v0.2C.json`, and `research/runtime_evidence/SFO_REIKLAND_CHAOS_REPLAY_DIVERGENCE_CALIBRATION_v0.2C.json` as frozen exact limiting-result evidence. The replay did not emit `BATTLE_COMPLETE`; process exit, replay-overlay exhaustion, and the zero-loss replay-exit screen are not terminal outcome evidence. Preserve the original defeat as `OWNER_ATTESTED`, the replayed state as `UNVERIFIED_NONTERMINAL`, and authority as `NO_ORDERS`. Do not request another playback of this exact replay.

## v0.2B historical preparation rule

Treat `synthetic_lab/corpora/sfo_chaos_defeat_visual_preparation_v0.2B.json`, `runtime_probe/fixtures/sfo_chaos_defeat_replay_contract_v0.2B.json`, and `research/runtime_evidence/SFO_REIKLAND_CHAOS_DEFEAT_CAPTURE_READINESS_v0.2B.json` as frozen exact preparation inputs. The replay/video bytes remain private and excluded. The defeat outcome is owner-attested and visually consistent, not visually grade-confirmed. Do not promote visual diagnoses into causality or optimal-policy labels. The only open step is one exact read-only replay capture; accept the exact shadow pack only under its frozen hash and byte-identical battle-script contract.

## v0.2A release rule

Treat the v0.1Z recovered SFO fixture, the two v0.2A dense replay corpora, and `synthetic_lab/corpora/sfo_reikland_dual_replay_visual_alignment_v0.2A.json` as frozen observed inputs. Do not add raw logs, checkpoints, saves, replays, videos, frames, personal paths, generated packs, or Workshop content. Preserve replay binary identity, runtime battlefield identity, and visual display identity as separate source fields. Command events and visible movement are observation only, never acknowledgement or causal execution. The exact two-replay visual-alignment cohort is closed; no additional Ubersreik or Marienburg capture is required.


## Mission

Build the strongest practical, enjoyable, explainable, and maintainable AI/gameplay enhancement possible for the owner's personal Total War: WARHAMMER III experience.

The product is a vanilla-compatible core. SFO: Grimhammer III is the first and highest-priority optional compatibility profile. DeepWar AI and Hecleas AI Overhaul are audited reference environments, not required dependencies.

## Canonical authority

Repository state overrides chat memory. Read the research files and active gate before changing architecture.

## Capability labels

Every game interaction must be classified as `CONTROL`, `INFLUENCE`, `OBSERVE`, `UNAVAILABLE`, or `UNVERIFIED`.

Do not label a database tweak, difficulty bonus, mechanic bypass, direct resource grant, scripted recovery, or state correction as a replacement for Creative Assembly's internal AI.

## Evidence labels

Use `OBSERVED`, `REPLICATED`, `SUPPORTED`, `HYPOTHESIS`, `ANALOGY`, `SPECULATION`, `LIMITING_RESULT`, or `INVALIDATED`.

## Core invariants

- Project-owned canonical state.
- Observation, belief, objective, candidate action, selected order, acknowledgement, and outcome are distinct.
- Any model or LLM is replaceable and bounded.
- All orders require legality, capability, fairness, and budget validation.
- Deterministic fallbacks exist.
- Raw evidence is preserved.
- Runtime and offline research remain separate.
- Synthetic success is never treated as real-game proof without calibration.
- Reality Gate observer probes do not mutate campaign or save state; persistence probes are separate and explicitly enabled.
- Environment-specific assumptions remain behind optional profiles.
- Unknown profile hashes revoke certification and fall back safely.
- Third-party code and assets require provenance and license records.
- Creative Assembly assets, Workshop packs, saves, replays, and personal artifacts stay out of Git.

## Engineering preference

Use the simplest system that passes the benchmark: constraints → utility → state machines → assignment → behavior trees → HTN/search → learning → LLM proposals.

## Completion

A gate passes only with preserved evidence, repeated tests where appropriate, regression coverage, hashes/configuration, and an explicit statement of remaining uncertainty.

## v0.1Y-r5 watcher rule

Detached helper readiness must never depend on equality between a launcher PID and a child runtime PID. Use an invocation-specific nonce in an atomic status file, preserve process IDs only as diagnostics, and capture private stderr/stdout for actionable failures.
