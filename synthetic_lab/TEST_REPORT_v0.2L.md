# SyntheticLab Test Report — v0.2L Native Visible Directional Churn Preregistration

**Date:** 2026-08-02

Full SyntheticLab regression: **164/164 PASS**.

Focused v0.2L adversarial matrix: **9/9 PASS**.

Added invariants include:

- two non-overlapping episodes required for the primary repeated signal;
- one A→B→A episode remains candidate-only;
- exact observed-context changes exclude primary windows;
- visibility loss and turn gaps censor/exclude rather than create churn;
- ambiguous direction cannot become a region proxy;
- privileged foreign visibility fails closed;
- translation and input ordering preserve semantic output;
- cohort comparison is descriptive and cannot authorize a project planner.

Authority remains `NO_ORDERS`; application remains `PROHIBITED`.
