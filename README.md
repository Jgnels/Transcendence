## v0.2R offline engineering — GITHUB-CANONICAL

v0.2R is now promoted to GitHub `main` on top of the exact sealed v0.2Q evidence. It adds three isolated offline capabilities: a deterministic Native Behavior Replay Lab, a fail-closed single-row native CAI Ablation Factory, and an active-document native-first architecture audit. GitHub canonicalization completed through PR #1 (sealed v0.2Q cutover) and PR #2 (exact validated v0.2R tree); canonical `main` is merge commit `fae5ccdf44b98823182558c96d2f4c87218ac54c` with tree `3f4b8a721eec479a023561e5324fb4e8f48e8193` before this documentation-only closeout. No fresh Stage-A SFO replication has been consumed; no native row experiment is earned; no real ablation pack is committed or installed. The game-side Lua probes remain frozen. v0.2Q remains the authoritative evidence milestone and its fresh Stage-A SFO replication remains the next evidence-gathering step. Authority remains `NO_ORDERS`; application authority remains `PROHIBITED`.

## Current continuation — v0.2Q

The first fresh SFO benchmark is complete and reproducible. Its frozen v0.2P territorial branch is descriptive worsening: 39 eligible territorial windows, 18 reversal candidates, 46.1538%, zero repeated territorial forces, above the frozen vanilla 22.2222–31.7073% cluster-sensitivity envelope. Recovery remains negative at 14.2857%. Causal review blocks row attribution because eligible faction composition differs strongly across campaigns and the six-faction shared subset reverses direction. No row ablation is earned. v0.2Q stages one fresh SFO replication first; another vanilla campaign is conditional on replicated SFO elevation. See `research/NATIVE_SFO_BENCHMARK_ADJUDICATION_v0.2Q.md`, `research/NATIVE_SFO_REPLICATION_PREREGISTRATION_v0.2Q.md`, and `gates/GATE_0_NATIVE_SFO_REPLICATION_v0.2Q.md`. Application remains native-first and `NO_ORDERS / PROHIBITED`.

**Active-document note:** v0.2Q is the only current continuation in this README. Older milestone/implementation sections below are retained as historical evidence; they do not reactivate v0.2G/v0.2I or expand application authority.

## Historical continuation — v0.2P

The active gate is the pre-SFO mechanistic precommitment. v0.2P preserves the v0.2O matched SFO campaign protocol but strengthens offline interpretation before any SFO behavior trace: faction-cluster diagnostics, a frozen vanilla leave-one-faction-out envelope (0.222222–0.317073), direction-aware post-SFO branching, and row-footprint-constrained mechanism nominations. No game-side Lua changed; application remains native-first and `NO_ORDERS / PROHIBITED`. See `research/NATIVE_SFO_MECHANISTIC_PRECOMMITMENT_v0.2P.md`.

## Historical milestone: v0.2O Matched SFO Native-CAI Behavior Benchmark

The fresh owner vanilla v0.2N cohort is complete and independently reproducible. Recovery had sufficient exposure but did not cross its frozen 20% attacker-side re-entry threshold (38 damaged army-turns, 6 attacker-side reentries, 15.7895%). The formal temporal endpoint fired (106 eligible battle-free stable-context windows, 18 reversal candidates, one repeated force), but the sole repeated force was the non-territorial Rogue Pirate faction `wh2_dlc11_cst_rogue_grey_point_scuttlers`. Owner telemetry is consistent with a fixed sea-patrol explanation, so **no ordinary native-CAI row ablation is authorized from v0.2N alone**.

v0.2O prospectively freezes territorial (stable non-empty owned-region set) as the primary strategic temporal cohort for a fresh matched SFO run while preserving every v0.2N movement, battle, stability and recovery threshold. The SFO profile is bound to Workshop `2792731173` / `sfo_grimhammer_3_main.pack` SHA-256 `ae20a0a08037aa3ebcbe7461d2589fc28dc15a5fe5d9ca4c0a9d0ff0a26da603`, plus the unchanged privileged diagnostic probe and no other mod. SFO pack bytes remain owner-local.

Authority remains `NO_ORDERS`; privileged telemetry remains `application_eligible=false`; application authority remains `PROHIBITED`; native WH3 CAI remains the default planner/executor. See `research/NATIVE_BEHAVIOR_STUDY_CAUSAL_REVIEW_v0.2O.md`, `research/NATIVE_SFO_MATCHED_BEHAVIOR_BENCHMARK_PREREGISTRATION_v0.2O.md`, and `gates/GATE_0_NATIVE_SFO_MATCHED_BEHAVIOR_BENCHMARK_v0.2O.md`.

## Historical milestone: v0.2N Fresh Native CAI Behavior Study Preregistration

The successful v0.2M-r1 owner vanilla capture **qualified the privileged read-only diagnostic channel**: 1,588 paired AI faction-turns, 2,353 matched army-turn pairs, 967 moved army-turns, 426 trajectory pairs, zero capability failures and zero incomplete normal AI faction turns. The r1 `rebels` pseudo-faction skip also survived six complete AI cycles.

v0.2N now freezes the first fresh confirmatory study before new behavior data. It adds a read-only `ScriptEventPendingBattle` participant channel, structurally rejects older traces that do not declare `battle_participant_telemetry=true`, and preregisters two endpoints: damaged-army attacker-side battle re-entry and repeated non-overlapping battle-free stable-context heading reversals. The old v0.2M-r1 cohort is exploratory only and cannot confirm either endpoint.

Fresh owner protocol: vanilla + diagnostic probe only, new Karl Franz IE Legendary/VH campaign, normal play/autoresolve allowed, 11 consecutive Reikland turn-start markers (10 complete AI cycles). Privileged telemetry remains `application_eligible=false`; authority remains `NO_ORDERS / PROHIBITED`; native WH3 CAI remains the default planner/executor.

See `research/NATIVE_DIAGNOSTIC_BEHAVIOR_STUDY_PREREGISTRATION_v0.2N.md` and `gates/GATE_0_NATIVE_DIAGNOSTIC_BEHAVIOR_STUDY_v0.2N.md`.

## Historical milestone: v0.2M Native CAI Diagnostic Telemetry Qualification

The first preregistered player-visible churn run produced zero eligible windows because visibility censoring/stationarity eliminated all 187 candidates. Rather than weaken the endpoint or waste an SFO repetition, v0.2M prepares a separate **research-only omniscient diagnostic plane**. It observes complete AI faction turns without issuing orders and can never be an application input. The next live step is a six-turn vanilla telemetry-density qualification, not an AI-quality verdict.

## Historical offline gate — v0.2K player-visible native behavior acquisition

The native-first reconciliation now has a visibility-safe acquisition layer. `runtime_probe/tools/run_native_visible_behavior.py` converts existing shadow logs into `NATIVE_CAI_PLAYER_VISIBLE_PARTIAL_TRACE_V1` without reading hidden foreign force lists or importing the old strategic assignment pipeline. `runtime_probe/tools/reprocess_native_visible_behavior_evidence.py` reprocesses the preserved owner turns 4–7 into `research/runtime_evidence/PLAYER_VISIBLE_NATIVE_BEHAVIOR_REPROCESS_v0.2K.json`. Across seven visible foreign factions it contains 37 comparable force intervals: 36 position-stable, one movement interval, and that movement is directionally ambiguous. These are observation statistics, not an AI-quality verdict. Full-faction coverage, recovery, reserves, exclusivity, memory, hysteresis, and native task identity remain unavailable under the normal player-visible boundary. No owner WH3 run is required for this gate; authority remains `NO_ORDERS`, application `PROHIBITED`. **v0.2K certification: 159/159 SyntheticLab + 164/164 Runtime Probe = 323/323 PASS.**

## 2026-08-02 architecture-reconciliation status

The owner-validated code baseline remains `v0.2I-r3`, but the **working application architecture changed after a multi-model native-CAI/prior-art review**. Native WH3 CAI is now the default strategic planner/executor; Transcendence is evaluator/tuner first, with narrowly bounded correction only after measured native failure. Assignment-derived `v0.2I` is paused. See `research/NATIVE_CAI_RECONCILIATION_DECISION_2026-08-02.md` and the active `gates/GATE_0_NATIVE_CAI_RECONCILIATION.md`.

## Historical offline gate — v0.2J native behavior detector

The row-level native-CAI reconciliation is now followed by a **trajectory-only failure detector**, not another strategic controller. v0.2J measures threatened-front coverage/response latency, observed recovering-army offensive engagements, a healthy unengaged home-zone buffer proxy, stationary-position proxies, and directional retarget/reversal proxies. It explicitly refuses to infer native assignment exclusivity, persistent task memory, or hysteresis from movement alone. The adversarial matrix passes 9/9 cases; direct regression is 155 SyntheticLab + 160 Runtime Probe = 315 tests. Authority remains `NO_ORDERS`; application remains `PROHIBITED`. No owner WH3 run is requested until a separate read-only observation acquisition contract is complete.

Rebuild the detector matrix with:

```powershell
$env:PYTHONPATH = "synthetic_lab"
python -m transcendence_lab.cli native-behavior-matrix `
  synthetic_lab\scenarios\native_cai_behavior_adversarial_matrix_v0.2J.json
```

## Historical implementation — v0.2I-r3 (paused)

A repeated owner-runtime run now proves three distinct boundaries: the real/UI poll executes, the shared parser accepts the diagnostic vocabulary, and the sidecar reaches `WAITING_FOR_QUERY_RESULTS`; however WH3 still does not observe the runtime-created request file (`request_seen=False`). v0.2I-r3 therefore stops depending on inbound runtime file reads and adds a fail-closed **prelaunch embedded-request retry**. It consumes the exact prior saved plan/request, builds a session-local PFH5 probe with that bounded request embedded, binds the generated pack hash in the SFO preflight, and later accepts results only if the new live snapshot independently reproduces the same plan. No order or save mutation is introduced.

## Historical implementation — v0.2I-r2 (paused)

The second owner-runtime feasibility attempt proved the r1 real/UI timer fires, then failed closed locally because the shared parser did not allow the two r1 diagnostic events. v0.2I-r2 fixes that parser-contract drift, adds an emitter→parser regression, binds the parser hash into the live-preparation artifact, and leaves the exact game-side probe unchanged. The live gate remains open and still requires only one read-only SFO campaign-map observation with no turn advance or order.

# Transcendence — WH3 Personal AI & Gameplay Project

## Historical implementation — v0.2I-r1 (paused)

The first owner-runtime v0.2I feasibility attempt successfully reached exact SFO + probe preflight, first-tick snapshot, current strategic assignment, v0.2H plan generation, and a five-query request, but returned no query result/rejection/packet-end while the map was intentionally idle. That run is preserved as a limiting transport result.

The r1 hotfix changes only the asynchronous request scheduler: one immediate read-only poll plus a 250 ms UI-synchronized `repeat_real_callback`, with one-shot `FEASIBILITY_POLL_TICK` and `FEASIBILITY_REQUEST_SEEN` diagnostics. The whitelist, 16-query cap, exact plan/turn/assignment bindings, collector reconstruction, and authority boundary are unchanged.

Regression target: **150 SyntheticLab + 152 Runtime Probe = 302 direct tests**. Authority remains `NO_ORDERS`; application remains `PROHIBITED`. After installation, the next gate is one repeat of the same bounded read-only SFO campaign-map observation; no battle, replay, turn advance, movement, attack, or save is required.

## Historical implementation — v0.2I (paused)

v0.2I closes the **offline preparation** for the first live campaign strategic-feasibility observation. A dedicated campaign-only probe captures one observer-safe snapshot on first tick, a local sidecar runs the canonical v0.2E→v0.2H pipeline to choose at most one current assignment, and the game executes only the resulting hard-coded read-query whitelist. No current assignment means no request.

The runtime executor is capped at 16 exact plan-bound queries and contains no movement, attack, stance mutation, garrison transfer, action-point manipulation, pathfinding mutation, save write, or generic order adapter. The collector independently rebuilds the plan from the raw snapshot before accepting results and exports a deterministic public-safe packet without raw logs or personal paths.

Offline regression target: **150 SyntheticLab + 151 Runtime Probe = 301 direct tests**. Authority remains `NO_ORDERS`; application remains `PROHIBITED`. The next distinct gate is one owner SFO campaign-map observation; a battle, replay, turn advance, army movement, or save is not required.

Prepare/collect after installing the canonical release:

```powershell
.\runtime_probe\tools\prepare_campaign_feasibility_observation.ps1
# configure exactly SFO + transcendence_campaign_feasibility_probe.pack, type READY,
# then load a current SFO campaign and remain on the campaign map briefly
.\runtime_probe\tools\collect_campaign_feasibility_observation.ps1
```

## Historical implementation — v0.2H (research/capability infrastructure)

v0.2H closes the first campaign strategic feasibility/action-authority gate **offline**. It converts v0.2G shadow assignments into bounded query plans over documented WH3 model-hierarchy interfaces while keeping every owner-runtime result unobserved and every mutation surface prohibited.

The preserved turn-7 Reikland assignment produces one eight-query historical plan bound to character CQI `120`, force CQI `65`, current stance, and the observer-safe Marienburg centroid. The centroid remains a point reference only. REGION targets may resolve to their exact settlement interface for reachability queries, but neither case creates an attack order or proves route/action legality.

Frozen outputs:

- strategic feasibility envelope digest `b6519eb75d141642a925f919f20fe1cf7300655b40300660dd1aabf8a873f553`;
- adversarial matrix digest `08614e2a7e292b495ecb93a54375bc7782b7e7b7a02502d1c4e8a56d59f07cea`;
- 5/5 plan cases, 5/5 metamorphic checks, synthetic observation checks, and stale/foreign/authority tamper checks pass;
- authority remains `NO_ORDERS`, application `PROHIBITED`.

Rebuild with:

```powershell
$env:PYTHONPATH = "synthetic_lab"
python -m transcendence_lab.cli strategic-feasibility `
  research\runtime_evidence\CAMPAIGN_TURNS_4_7_REPROCESS_v0.1J.json `
  research\runtime_evidence\REIKLAND_CAMPAIGN_STRATEGIC_FORCE_ALLOCATION_v0.2G.json
python -m transcendence_lab.cli strategic-feasibility-matrix `
  synthetic_lab\scenarios\campaign_strategic_feasibility_adversarial_matrix_v0.2H.json
```

The next distinct gate is a batched read-only live query observation on a suitable current campaign state. Historical turn 7 does not need to be recreated.

## Historical implementation — v0.2G (evaluator only)

v0.2G adds deterministic theater-to-army shadow allocation and a multi-turn commitment lifecycle above v0.2F. Force-requiring strategic priorities can be mapped to unique planner-eligible controlled armies while preserving recovery, reserve capacity, critical-overflow sources, aggression vetoes, and explicit shortages. Consecutive observations retain valid commitments and prevent geometry-driven reassignment until disclosed review conditions are met.

Assignments remain `SHADOW_PROPOSED_NOT_EXECUTED`; geometry is `NOT_EVALUATED_GEOMETRIC_REFERENCE_ONLY`; authority is `NO_ORDERS`; application is `PROHIBITED`. No campaign path, movement, action, acknowledgement, execution, or outcome claim is made.

Frozen outputs:

- force-allocation digest: `1ea40164577d918e349bd7f2873c0afb4a24ecf98f0e437c244f0ada12ea9f72`;
- assignment/commitment matrix digest: `61fa5cc91ba3c4a6f96c4e43f3613b01e6b484e606192c3039b33134f821e353`.

Rebuild with:

```powershell
$env:PYTHONPATH = "synthetic_lab"
python -m transcendence_lab.cli strategic-force-allocation `
  research\runtime_evidence\CAMPAIGN_TURNS_4_7_REPROCESS_v0.1J.json `
  synthetic_lab\scenarios\tier2_late_game_pressure.json `
  research\runtime_evidence\REIKLAND_CAMPAIGN_STRATEGIC_CHALLENGE_ENVELOPE_v0.2E.json `
  research\runtime_evidence\REIKLAND_CAMPAIGN_STRATEGIC_THEATER_PORTFOLIO_v0.2F.json
python -m transcendence_lab.cli strategic-assignment-matrix `
  synthetic_lab\scenarios\campaign_theater_assignment_commitment_adversarial_matrix_v0.2G.json
```

## Previous implementation — v0.2F

v0.2F adds the first bounded strategic/theater priority portfolio above the observer-safe v0.2E challenge envelope. It does not assign armies or issue campaign orders. The portfolio represents front stabilization, siege-relief coverage, field-force recovery, coherent-rival containment, fragmented-pressure management, reserve preservation, and low-pressure consolidation under a six-record budget with explicit critical overflow. Local crisis or total-force recovery vetoes new aggressive commitment.

Frozen outputs:

- strategic portfolio digest `13bd4e418a4056e570a656c6821ee002412c8e3d90f32d61ba2cf333538c5c22`;
- adversarial matrix digest `e1c1b8408e3b405bbedbcef01d1802e10206a71c38a9264337aa8cdb62959125`;
- 12/12 adversarial cases and 6/6 metamorphic checks;
- 126 SyntheticLab + 141 Runtime Probe = 267 direct regression tests;
- hidden enemy state, player/NPC labels, input ordering, coordinate translation, strength scale, and empty war edges are semantically inert;
- authority remains `NO_ORDERS`, application `PROHIBITED`.

Rebuild with:

```powershell
$env:PYTHONPATH = "synthetic_lab"
python -m transcendence_lab.cli strategic-portfolio `
  synthetic_lab\scenarios\tier2_late_game_pressure.json `
  --observed-report research\runtime_evidence\CAMPAIGN_TURNS_4_7_REPROCESS_v0.1J.json `
  --challenge-envelope research\runtime_evidence\REIKLAND_CAMPAIGN_STRATEGIC_CHALLENGE_ENVELOPE_v0.2E.json
python -m transcendence_lab.cli strategic-portfolio-matrix `
  synthetic_lab\scenarios\campaign_strategic_theater_portfolio_adversarial_matrix_v0.2F.json
```

## Previous implementation — v0.2E

v0.2E reconnects Transcendence to its primary product problem: late-game campaign challenge collapse. It adds a deterministic, observer-safe strategic-challenge benchmark envelope without adding campaign order authority. The evaluator distinguishes coherent visible rival **candidates**, fragmented pressure, exposed/covered fronts, visible force concentration, and recovery load while refusing to infer anti-player bias, diplomacy intent, native CAI intent, or campaign quality from a snapshot.

Frozen outputs:

- campaign challenge envelope digest `4cbc5cb253b5cc16966807e889e4c05354a284100cd8f237b90b493281bc9423`;
- 8/8 adversarial cases and 5/5 metamorphic checks;
- 115 SyntheticLab + 139 Runtime Probe = 254 direct regression tests;
- full repository validation: 368 canonical hashed files;
- matrix digest `4acfa02699cd9f78eace303889d2b7bba274b82b65582b2ea21f3cd70690e3df`;
- hidden enemy state and player-vs-NPC labels are semantically inert;
- authority remains `NO_ORDERS`, application `PROHIBITED`.

Rebuild with:

```powershell
$env:PYTHONPATH = "synthetic_lab"
python -m transcendence_lab.cli campaign-challenge `
  synthetic_lab\scenarios\tier2_late_game_pressure.json `
  --observed-report research\runtime_evidence\CAMPAIGN_TURNS_4_7_REPROCESS_v0.1J.json
python -m transcendence_lab.cli campaign-challenge-matrix `
  synthetic_lab\scenarios\campaign_challenge_adversarial_matrix_v0.2E.json
```



## Previous implementation — v0.2D

v0.2D closes the first cross-corpus tactical policy gate without another WH3 run. It binds vanilla Eilhart, SFO Ubersreik, SFO Marienburg, and the nonterminal SFO Chaos replay-divergence trace into one deterministic `NO_ORDERS` policy envelope. Supported safeguards are separated from single-corpus hypotheses, terminal evidence fails closed, severe high-value-asset review is ratio-based, and the known Eilhart runtime-identity alias is preserved explicitly instead of being silently rewritten.

Frozen outputs:

- policy digest `4fe0d769dee3649bd2f4f9ce2db364ae6dbd75e045f618091096ff3a67de3fee`;
- 17/17 adversarial scenarios and 3/3 metamorphic checks;
- matrix digest `5ab0a7e9668b130811d1c10f6a0fe866e2933e8cc7370361a1b9d323bf1391f6`;
- 105 SyntheticLab + 137 Runtime Probe = 242 direct tests.

Rebuild the frozen policy and matrix with:

```powershell
$env:PYTHONPATH = "synthetic_lab"
python -m transcendence_lab.cli cross-corpus-policy `
  synthetic_lab\corpora\battle4_eilhart_observed_dense_v2.json `
  synthetic_lab\corpora\sfo_ubersreik_observed_dense_v0.2A.json `
  synthetic_lab\corpora\sfo_marienburg_observed_dense_v0.2A.json `
  synthetic_lab\corpora\sfo_chaos_replay_stream_observed_dense_v0.2C.json `
  research\runtime_evidence\SFO_REIKLAND_DUAL_REPLAY_TACTICAL_CALIBRATION_v0.2A.json `
  research\runtime_evidence\SFO_REIKLAND_CHAOS_REPLAY_DIVERGENCE_CALIBRATION_v0.2C.json
python -m transcendence_lab.cli policy-matrix `
  research\runtime_evidence\REIKLAND_CROSS_CORPUS_TACTICAL_POLICY_ENVELOPE_v0.2D.json `
  synthetic_lab\scenarios\tactical_policy_adversarial_matrix_v0.2D.json
```

## Previous implementation — v0.2C

v0.2C closes the exact `An Ogre's Folly` replay gate as a **limiting result**. The read-only SFO capture produced a valid dense nonterminal stream—115 detail samples, 637 aggregate records, 23 canonical units, and 89 command events—but no `BATTLE_COMPLETE`. The replay-end overlay and subsequent zero-loss screen are not the original battle result. The owner-attested live defeat and exact grade therefore remain separate from the replayed simulation.

SyntheticLab now supports nonterminal crisis and lower-bound loss diagnosis while excluding this trace from terminal-outcome labels. Runtime reporting no longer marks incomplete sessions as observed terminal outcomes, and the collector stabilizes late log writes after WH3 exits without treating process exit as completion.

Validation target: **95 SyntheticLab + 135 Runtime Probe = 230 tests**. Canonical file count is sealed by `SHA256SUMS.txt`.

Run the deterministic calibration:

```powershell
.\synthetic_lab\tools\run.ps1 sfo-chaos-replay-adjudication `
  runtime_probe\fixtures\sfo_chaos_replay_stream_capture_v0.2C.json `
  synthetic_lab\corpora\sfo_chaos_replay_stream_observed_dense_v0.2C.json `
  runtime_probe\fixtures\sfo_chaos_replay_unit_findings_v0.2C.json
```

## Previous implementation — v0.2B

v0.2B bound the replay/video and prepared the one exact read-only playback. That capture has now been completed and adjudicated; no repeat is required.

## Previous implementation — v0.2A

v0.2A closes the exact Ubersreik/Marienburg replay-visible calibration gate. It binds two dense SFO replay corpora to three private owner video hashes without committing video, frames, replays, raw logs, paths, or game assets. SyntheticLab now preserves force-completeness, outcome-versus-cost, local-crisis, terminal-abstention, and identity-provenance rules under `NO_ORDERS`.

The visually confirmed outcomes are Ubersreik `Decisive Victory` and Marienburg `Pyrrhic Victory`. Marienburg lasted 2.100861 times as long and carried a 2.341317-times larger observed local casualty lower bound, demonstrating that victory status alone is not an adequate tactical-quality benchmark. Runtime identity remains separately preserved as `Battle of Eilhart — Reikland vs Empire Secessionists` rather than silently rewritten.

Validation target: **85 SyntheticLab + 126 Runtime Probe = 211 tests; 331 canonical hashed files.**

Run the exact calibration:

```powershell
.\synthetic_lab\tools\run.ps1 sfo-dual-replay-calibration `
  synthetic_lab\corpora\sfo_reikland_dual_replay_visual_alignment_v0.2A.json `
  synthetic_lab\corpora\sfo_ubersreik_observed_dense_v0.2A.json `
  synthetic_lab\corpora\sfo_marienburg_observed_dense_v0.2A.json
```

## Previous implementation — v0.1Z-r1

v0.1Z-r1 freezes the recovered exact SFO owner session as a path-free observed baseline: six Reikland turns, three completed ordinary land battles, a campaign return after each battle, 130 exact append-only checkpoints, 698 detail samples, 4,000 alliance aggregates, 683 command events, 1,025 selections, and 80 canonical units. Command traffic remains `OBSERVED_NOT_ACKNOWLEDGED`; order issue, acknowledgement, causal execution, tactical superiority, and unobserved battle types are not promoted.

The release also repairs the collector defects exposed by the run and the owner-machine v0.1Z installer regression: schema-2 transition recognition, atomic/nonzero watcher control files, checkpoint-manifest authority, deterministic verified Python evidence ZIPs, and in-process PowerShell rollback. It adds an exact read-only SFO replay deep-dive workflow for the preserved Ubersreik and Marienburg replays.


A public code repository for a personal Total War: WARHAMMER III enhancement project.

## Product boundary

Transcendence is a **vanilla-compatible core**. SFO: Grimhammer III is the first and highest-priority optional compatibility profile, not a required dependency. DeepWar AI and Hecleas AI Overhaul are reference environments used to study existing approaches.

The north star is to preserve strategic danger and meaningful opposition after the player becomes powerful without replacing difficulty with arbitrary omniscience, universal anti-player targeting, or invisible stat inflation. Campaign strategy and real-time battles are both first-class parts of the intended experience.

## Confirmed owner baseline

- pure vanilla WH3 v8.1.1 build 48122.4194776;
- Immortal Empires;
- Karl Franz / Reikland;
- Legendary campaign difficulty;
- Very Hard battle difficulty;
- battle realism;
- Ultra unit size.

## Historical implementation summary — through v0.1Y-r5

SyntheticLab now carries the bounded tactical chain through semantically qualified point evidence, guarded action-packet abstention, simultaneous endpoint reservation, and deterministic full-pipeline adversarial/scaling audit. Every layer remains `NO_ORDERS`; no route, formation, command legality, acknowledgement, execution, or outcome claim is introduced.

The SFO combined-session workflow is prepared with append-only checkpointing, exact environment binding, privacy-safe export, deterministic `used_mods.txt` discovery, and prelaunch probe-binding hardening. Exact and safely decorated local-probe entries bind to the installed pack; a launcher file that materializes only SFO before WH3 starts is labeled `DEFERRED_RUNTIME_MARKER` and can be certified only by the exact prepared-pack hash plus observed runtime markers. Extra mods, duplicate aliases, wrong hashes, and absent runtime markers fail closed. The live five-turn/two-battle SFO continuity gate remains open.

Runtime and lab evidence now include:

- strictly replicated read-only observer and isolated one-value persistence proofs;
- corrected campaign observation for turns 4–7 and append-only cross-runtime logging prepared for the next broad campaign session;
- an exact 739.6-second Battle of Eilhart replay captured with 249 dense unit samples, 1,477 alliance aggregates, 120 sampler heartbeats, 37 stable units, zero identity aliases, and 397 command events;
- a public-safe dense corpus plus eight visibility-filtered battle-state slices;
- strict `BATTLE_TRACE_TACTICAL_INPUT_V2`, `TACTICAL_STATE_VISIBILITY_SAFE_V2`, and `TACTICAL_STATE_TRAJECTORY_V2`;
- role-specific commander, artillery, ranged, cavalry, and frontline preservation policies;
- deterministic support, visible-pressure, danger, asset-value, recoverability, collapse-risk, and marginal-engagement proxies;
- independent local-stability, visible-enemy-collapse, and combined tactical-phase state machines;
- ten guarded decision-opportunity types, including pursuit termination and reformation;
- stable opportunity lifecycle identity, with 28 real transitions and 11 continuing priorities versus 397 observed owner command events, labeled `REFERENCE_ONLY_NOT_CAUSAL`;
- four `PLAUSIBLY_PREVENTABLE_SEVERITY` diagnoses and seven insufficient-evidence diagnoses, all `UNVERIFIED`;
- a 24-case deterministic adversarial suite: 18 malformed inputs rejected and 6 metamorphic invariants passed;
- `TACTICAL_PRIORITY_PORTFOLIO_V1`, which batches equivalent unit concerns before applying the six-priority limit and preserves critical overflow explicitly;
- a 16-scenario `CONTROL_SYNTHETIC` baseline matrix with 16/16 role-aware contract passes and one unit-scale equivalence check;
- preserved before-state evidence showing legacy critical-source coverage of 66.6667% and 60% under saturation, corrected to 100%;
- `TACTICAL_OBJECTIVE_ASSIGNMENT_V1`, which maps portfolio sources into self-directed and support objectives without emitting orders;
- explicit uncontrollable-subject, no-legal-responder, and higher-value resource-conflict results instead of fabricated feasibility;
- a 12-scenario assignment matrix plus unit-order, portfolio-order, and coordinate-translation invariants;
- Battle 4 assignment evidence with 50 objectives, 34 assignments, 16 explicit unfilled objectives, four resource conflicts, and zero double-booked units;
- `TACTICAL_TEMPORAL_SCHEDULE_V1`, with project-owned commitment/review/cooldown windows, explicit cancellation and continuity-reset events, and no causal credit for observed objective disappearance;
- a 12-scenario temporal lifecycle matrix with 12/12 contracts and three order/translation invariants;
- Battle 4 temporal evidence showing 34 starts, zero continuations, 18 gap-reset events across active plans, and zero double-booked actors—an explicit sparse-cadence limiting result;
- repository validation that ignores generated paths relative to the checkout rather than accidentally ignoring checkouts beneath a `tmp` or `build` ancestor;
- `TACTICAL_FEASIBILITY_ENVELOPE_V1`, with at most three bounded candidate points per active plan and exact-point-only query evidence;
- a 15-scenario feasibility matrix with 15/15 contracts and three invariance checks;
- Battle 4 feasibility evidence with 34 query-ready envelopes and 102 candidates, none falsely promoted from aggregate live data;
- a no-replay detailed re-export workflow for the exact preserved v0.1S private log;
- no campaign orders, battle orders, unitcontrollers, speed changes, save writes, damage/healing, ammunition mutation, visibility mutation, or randomness.

The dense replay observation gate remains closed as `OBSERVED_DENSE_SELECTION_LIMITING_RESULT`. Direct selection callbacks did not replay, so command-to-unit state matching remains `INFERRED_NOT_ACKNOWLEDGED`. No further Battle 4 replay is required.

Run full repository validation:

```powershell
.\tools\validate.ps1
```

Run the v0.1N decision framework with the dense command-reference corpus:

```powershell
.\synthetic_lab\tools\run.ps1 tier4r-shadow `
  synthetic_lab\corpora\battle4_eilhart_trace_slices_v1.json `
  --dense-corpus synthetic_lab\corpora\battle4_eilhart_observed_dense_v2.json
```

The canonical v0.1Q outputs add:

- `research/runtime_evidence/BATTLE4_EILHART_TACTICAL_TEMPORAL_SCHEDULE_v0.1Q.json`;
- `research/runtime_evidence/TACTICAL_TEMPORAL_SCHEDULE_MATRIX_REPORT_v0.1Q.json`.

The v0.1M through v0.1P outputs remain frozen historical evidence.

Build and stage all probes without modifying WH3:

```powershell
.\runtime_probe\tools\prepare_live_probe.ps1
```

## Repository visibility and safety

The repository is public because connected ChatGPT/Codex access does not reliably work with the owner's private repositories.

Never commit Creative Assembly assets, SFO or other Workshop pack contents, generated `.pack` files, saves, replays, private logs, personal paths, secrets, or API keys. Keep local-only artifacts beneath `local_inputs/`; build outputs remain beneath ignored `dist/`.

## Canonical workflow

1. Read `AGENTS.md`.
2. Read `research/CURRENT_STATE.md` and the active gate.
3. Use `prompts/CONTINUE_PROJECT.md` for autonomous continuation.
4. Preserve exact versions, hashes, seeds, and evidence labels.
5. Treat synthetic success and documentation claims as hypotheses until Reality Gate R0 observes WH3.

## v0.1S observed action-authority calibration

The exact read-only ordinary-battle probe produced one complete owner capture with 130 selection events, 89 selection-bound command windows, 1,287 state samples, raw true and false callback-position reachability results, ordered-position/current-target matches, movement, routing, control loss, and interruption evidence. It created no unitcontroller and issued no order. Command origin remains unresolved, state matches remain `NOT_ACKNOWLEDGED`, and outcomes remain unattributed.

The current capture is sufficient; no repeat run is required. The workflow remains available for a future explicitly justified capture:

```powershell
.\runtime_probe\tools\prepare_action_authority_capture.ps1
# Fight one ordinary single-player battle with only the action-authority pack enabled.
.\runtime_probe\tools\collect_action_authority_capture.ps1
.\runtime_probe\tools\rollback_action_authority_probe.ps1
```

## v0.1T bounded tactical feasibility

Run the offline envelope and matrix:

```powershell
.\synthetic_lab\tools\run.ps1 tier4r-feasibility `
  synthetic_lab\corpora\battle4_eilhart_trace_slices_v1.json
.\synthetic_lab\tools\run.ps1 feasibility-matrix `
  synthetic_lab\corpora\battle4_eilhart_trace_slices_v1.json `
  synthetic_lab\scenarios\tactical_feasibility_matrix_v0.1T.json
```

After owner installation, generate richer public-safe evidence from the already preserved v0.1S log without launching WH3:

```powershell
.\runtime_probe\tools\reexport_action_feasibility_capture.ps1
```


## v0.1U observed command-point semantic calibration

The exact no-replay detailed export exposed a semantic evidence defect: all 38 raw false reachability samples came from the zero-vector callback position of one `Move Orientation Width` window. They are preserved as raw telemetry but are not valid false results for an explicit destination.

The corrected qualified evidence is 16 explicit Move windows, 16 actors, and 191 point-query samples, all `QUERY_TRUE`. All 16 actors matched ordered position in the initial sample; 12 later showed movement. Visible `Attack Unit` targets matched current target in 48 of 49 actor-windows. These remain state observations, not acknowledgements.

Battle 4 remains unchanged at 34 query-ready envelopes and 102 unqueried candidate points. No additional gameplay run is required.


## v0.1V–v0.1X guarded pipeline batch

The cumulative offline batch adds:

- `TACTICAL_GUARDED_ACTION_PACKET_V1`, which authenticates exact state, schedule, feasibility, plan, packet, actor, and candidate identities before classifying a proposal as ready, deferred, or blocked;
- `TACTICAL_ENDPOINT_RESERVATION_V1`, which reserves only exact supported endpoint coordinates and enforces a 12 m development separation contract without claiming routes or formations;
- exact branch-and-bound arbitration through 16 ready packets, with explicit deterministic fallback above that structural bound;
- a 15-scenario guarded matrix, a 14-scenario reservation matrix, and six total representation invariants;
- a 256-case deterministic full-pipeline audit plus 12 scale cases through 160 ready packets;
- preserved regressions for duplicate-plan collapse and coordinate-derived optimization ties;
- fail-closed attacks for forged packet identities, duplicate actor identities, authority escalation, stale sources, and malformed geometry.

Battle 4 correctly produces 34 deferred guarded packets and zero endpoint reservations because none of its 102 generated candidates has candidate-level query evidence. These layers remain shadow-only and do not require another gameplay run.

Final cumulative v0.1X validation: **73 SyntheticLab tests, 81 Runtime Probe tests, 154 total tests, and 286 canonical hashes**.

## v0.1Y SFO combined-session preparation

v0.1Y prepares one exact SFO campaign+battle observation cohort with full SFO/WH3/probe/load-order hashing, automatic append-log prefix checkpoints, current battle-marker compatibility, and a path-free public export. The offline control contains five campaign turn snapshots and two battle cycles. Live SFO continuity remains owner-machine evidence; no save-and-quit is required between battles. v0.1Y-r1 corrects the Windows PowerShell 5.1 handoff so `Confirm = $false` is bound in process rather than serialized through a child `powershell.exe -File` call.


## v0.1Y-r4 current WH3 launch-script grammar hotfix

Owner preflight established that the refreshed game-root `used_mods.txt` uses WH3 launch directives rather than only bare `mod` lines. The parser now accepts the strict subset `add_working_directory "<absolute Windows path>";` plus `mod "<pack filename>";`. Working-directory directives are validated but are not counted as active mods. Relative paths, traversal, duplicate working directories, unknown directives, duplicate packs, extra packs, and ambiguous SFO resolution remain failures. The `READY` confirmation is now trimmed and case-insensitive.

## v0.1Y-r5 — SFO watcher handshake repair

The exact SFO preflight now succeeds on the owner machine. v0.1Y-r5 replaces the implementation-dependent watcher PID equality check with a 256-bit session-token handshake and private startup diagnostics. The live five-turn/two-battle SFO continuity run remains the next evidence gate.
